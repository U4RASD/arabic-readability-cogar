# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0
mord>=0.7
pyarrow>=12.0
tqdm>=4.65
matplotlib>=3.7

# Arabic NLP (feature extraction)
sinatools==1.0.9

# Readability formulas
textstat>=0.7
# dalla-data-processing provides the Arabic Flesch adaptation and the rank
# binning used by the evaluation harness. Install from its repository.

# LLM raters (OpenAI-compatible clients) and Gemini
openai>=1.0
google-genai>=1.0

# CAMeL baselines, distillation
torch>=2.0
transformers>=4.40
camel-tools>=1.5

# TOML parsing on Python < 3.11
tomli>=2.0; python_version < "3.11"
